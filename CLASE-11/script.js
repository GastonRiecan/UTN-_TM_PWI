/* Operadores logicos */

/* 
NOT => Negacion (devuelve el valor opuesto booleano del dato)

OR => o logico (Selecciona entre 2 valores)

&&  => y logico

ORDEN : NOT, AND, OR.

*/

/* 
VARIABLES:

Sintaxis: <tipo_variable> identificador = dato


var nombre = 'pepe';


VAR
LET
CONST

Scope y block (bloques y alcance)


*/

/*  IF ELSE (Condicional) */

/* 


*/

let edad = prompt('Ingrese su edad: ');
let lenguaje = prompt('Ingrese su lenguaje favorito: ');
if (edad === 16 && lenguaje === 'JS') {
    console.log('Bienvenido al desarrollo web frontend');
} else {
    console.log( 'No se puede ser perfecto en todo');
}